# Pinned to what production runs: rebuilding a venv is part of deploying, and an
# unpinned rebuild is how a working deploy silently picks up a breaking release.
discord.py==2.7.1
python-dotenv==1.2.2
rcon==2.4.9
aiosqlite==0.22.1
aiohttp==3.13.3
